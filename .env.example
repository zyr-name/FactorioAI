# Copy to .env to override these defaults.
# Clients must use the same Factorio version as the server.
FACTORIO_VERSION=2.0.77
FACTORIO_PORT=34197
FACTORIO_DATA_DIR=./data
# Set true BEFORE creating a world to enable Space Age (clients need the DLC).
DLC_SPACE_AGE=false
