# Git workflow

- Create or switch to a feature branch before editing files. Use the `codex/`
  prefix unless the user requests a different branch name.
- Never edit or commit directly on `main`. Submit changes through a pull request.
- Keep commit messages clear and short, describing what that commit adds or
  changes. Avoid vague messages such as "updates" or "fixes".
